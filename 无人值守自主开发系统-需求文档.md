# 需求文档：基于 Grok Build 的无人值守自主开发系统

> 版本：v1.0 ｜ 日期：2026-08-07 ｜ 状态：需求定义

## 一、需求背景

当前研发从 TAPD 需求到 GitLab MR 的开发环节需要工程师全程手动介入。希望基于 Grok Build（终端 AI 编程 Agent）改造出一套**无人值守的自主开发系统**：AI Agent 自动承接定义清晰的需求，独立完成开发、测试、自审、提交 MR，工程师只在 MR 环节做最终审批。

## 二、核心目标

构建 7×24 运行、无需人工触发的自主开发流水线：**TAPD 需求 → 自动开发 → 测试 → 自审 → GitLab MR**，人只保留 MR 审批这一个高价值决策点。

---

## 三、系统主流程

### 3.1 全流程总览

```
┌─ 拉取与调度 ────────────────────────────────────────────┐
│ ① 定时扫描 TAPD 拉取待开发需求                            │
│ ② 过滤（只留定义清晰的）→ 按"所属仓库"打标签 → 入共享队列 │
│ ③ worktree 空闲 → 原子 claim 匹配自己仓库的任务（加 lease）│
└──────────────────────────┬──────────────────────────────┘
┌─ 准备 ───────────────────▼──────────────────────────────┐
│ ④ 重置 worktree 到干净 main → 拉最新 → 基于 main 建分支   │
│ ⑤ 【设计/定界】理解需求 → 判断改哪个包、什么性质          │
│    → 确定要加载的 spec 分片清单                           │
│ ⑥ 加载 spec（核心层常驻 + 按包/主题动态加载）             │
└──────────────────────────┬──────────────────────────────┘
┌─ 开发-自检回环（根因驱动 + 升级边界，见 3.2）────────────┐
│ ⑦ 【TDD 编码】先写失败测试(Red) → 实现让其通过(Green)    │
│    → 重构(Refactor)。测试即需求的可执行表达               │
│         │                                                │
│         ▼                                                │
│ ⑧ 闸门一：跑全部测试 + HARD 机器检查（lint/CI）          │
│         ├─ 失败 ──► ⑦' 根因分析后定向修复                │
│         └─ 通过 ▼                                        │
│ ⑨ 闸门二：独立 Review Agent 审 SOFT 规则                 │
│         ├─ 打回 ──► ⑦' 根因分析后定向修复                │
│         └─ 通过 ▼                                        │
│ ⑩ 闸门三：对抗验证（找"会在什么情况下出错"）              │
│         ├─ 发现真问题 ──► ⑦' 根因分析后定向修复          │
│         └─ 通过 ▼                                        │
│                                                          │
│ ⑦' 【根因分析 → 定向修复】(替代盲目试错)                 │
│    1. 先诊断：这个失败的根本原因是什么？(读日志/复现/定位) │
│    2. 判断方向：是实现错、方案错、还是需求本身有阻塞？     │
│    3. 若方向明确 → 先写复现该问题的测试 → 再定向修 → 回⑧  │
│    4. 若诊断出"真阻塞"(需人工决策/缺依赖/超范围) → ⑬ 升级 │
│                                                          │
│  ★ 每轮修复前过【升级检查】(见 3.2)：只有诊断确认走不通    │
│    才升级，而非盲目试错到次数耗尽                         │
└──────────────────────────┬──────────────────────────────┘
┌─ 交付 ───────────────────▼──────────────────────────────┐
│ ⑪ 三道闸门全过 → 创建 GitLab MR → 回写 TAPD 状态=完成    │
│ ⑫ 【沉淀判断】分析本次修复回环中踩过的坑（见 3.3）        │
│    → 判断是否值得沉淀 → 值得的投入 updateSpec 待消化池    │
└──────────────────────────┬──────────────────────────────┘
┌─ 异常出口（升级求助，非"失败放弃"）──▼───────────────────┐
│ ⑬ 升级：根因分析已确认"当前无法自主推进"（需人工决策/缺   │
│    依赖/超范围/诊断不收敛）→ 标记需求=需人工 + 附上完整    │
│    诊断报告（根因是什么、试过哪些方向、卡在哪）+ 保留现场  │
│    （分支/diff/失败日志）+ 释放 worktree + 通知 → 进沉淀  │
└──────────────────────────┬──────────────────────────────┘
┌─ 异步闭环（独立于单任务）─▼──────────────────────────────┐
│ ⑭ updateSpec：异步消费 MR review / CI / 放弃案例的反馈    │
│    → 归纳 → 归片 → 产出人审的 spec 变更 MR（见 4.7）      │
└─────────────────────────────────────────────────────────┘
```

### 3.2 修复回环的根因驱动与升级边界

> **核心原则：闸门失败时，不盲目试错、也不轻易放弃。先做根因分析找对方向再定向修复；只有当诊断本身确认"当前无法自主推进"时才升级求助——升级不是失败，是把真正需要人的任务精确地交出去。**

#### 3.2.1 修复必须根因驱动（替代盲目试错）

每次闸门失败，回到步骤 ⑦' 时**强制先诊断、后动手**：

```
① 诊断根因：读失败日志 / 复现问题 / 定位到具体原因
       ↓
② 判断方向：
   ├─ 实现错了（逻辑/边界/类型）      → 方向明确，定向修
   ├─ 方案错了（设计层面走偏）        → 退回重新设计，别在错方案上补丁
   └─ 需求本身有阻塞（缺信息/需决策） → 不是代码能解决的 → 升级
       ↓
③ 定向修复（走 TDD）：
   先写一个能复现该问题的失败测试 → 再针对根因修 → 回闸门重验
```

**为什么这样能同时满足"不盲目"和"不轻易放弃"**：根因驱动的修复每轮都朝正确方向收敛，需要的轮次远少于盲目试错——所以大多数问题在少数几轮内就解决了，根本到不了升级边界。盲目试错才会撞上次数上限被迫放弃。

#### 3.2.2 升级边界（诊断驱动，非盲目计数）

升级的触发**不是"修了 N 次还没好"这种盲目计数**，而是由诊断结论驱动：

| 升级维度 | 触发条件 | 性质 |
|---------|---------|------|
| **诊断确认真阻塞** | 根因分析结论 = 需产品/人工决策、缺凭证/依赖、超出任务范围 | 主要出口，越早识别越好 |
| **诊断不收敛** | 已做深度根因分析仍找不到明确方向（不是没修够，是真不知道往哪修） | 说明超出 Agent 能力 |
| **方向反复推翻** | 换了 2 个方案方向仍不通（不是同一方案反复补丁，是方案层面反复） | 方案空间可能需要人介入 |
| **资源硬上限** | Token / 时间累计超预算（最后的兜底闸，防极端情况失控） | 兜底，正常不该靠它触发 |
| **改动失控** | diff 规模异常膨胀、越改越大 | 防止把代码改烂 |

> **和盲目计数的区别**：老方案是"修满 5 次就放弃"——可能第 5 次正朝对的方向走却被砍掉。新方案是"诊断说走不通才升级"——修多少次不重要，重要的是"是否还有明确的正确方向可走"。资源硬上限只作兜底，正常流程不该靠它触发。

#### 3.2.3 升级后的动作（步骤 ⑬）

升级 ≠ 失败。它是"带着精确诊断把任务交给人"：
1. 标记需求状态 = `需人工`
2. **附完整诊断报告**：根因是什么、判断的方向、试过哪些方案、每个为什么不通、卡点在哪、建议人怎么处理
3. **保留现场**：分支、当前 diff、完整失败日志
4. 释放 worktree 回工作池
5. 推送通知（企微/Slack）
6. 升级案例进入沉淀判断（"什么样的需求会走不通"是重要的规范信号）

### 3.3 沉淀判断（每个任务收尾都要做）

> **无论成功交付（⑫）还是升级求助（⑬），都要分析这次踩过的坑，判断是否需要沉淀成规范。**

沉淀判断的输入 = 本次修复回环中的**每一次闸门失败/打回记录 + 根因分析结论**。判断逻辑：

```
遍历本次踩过的每个坑（结合它的根因结论）：
   ├─ 是一次性偶发问题（拼写、手滑）？        → 丢弃，不沉淀
   ├─ 是显性风格问题（命名/格式）？          → 丢弃，交给 linter
   ├─ 是隐性铁律 + 可能重复出现？            → 标记"候选沉淀"
   │     ├─ 能机械判定 → 建议升级为 HARD 检查（lint/CI）
   │     └─ 只能靠判断 → 建议写入对应 spec 分片（SOFT）
   └─ 是升级案例暴露的能力边界？              → 记录"这类需求不适合自动化"
        │                                       （用于优化 4.1 的需求过滤规则）
        ▼
   候选沉淀项 → 投入 updateSpec 待消化池（不立即改 spec）
```

**关键边界**：
- 沉淀判断只**产出候选**，**不直接改 spec**——真正的沉淀由异步 updateSpec 闭环（⑭）统一消化、归片、产出人审 MR
- 单次任务里"当场解决的坑"不代表"要沉淀"——只有**重复出现、且属隐性铁律**的才值得沉淀，避免 spec 被一次性问题污染
- 沉淀判断本身也要有成本上限，不能为了"分析坑"又消耗大量资源

---

## 四、功能模块

### 4.1 需求拉取（TAPD 集成 · 新建 MCP）
- 定时扫描 TAPD 指定项目的待开发需求（间隔可配）
- 只筛选定义清晰、验收标准明确的需求，过滤复杂/模糊需求
- 读取每个需求的**所属仓库/模块**字段用于路由
- 回写需求状态（开发中 / 完成 / 需人工）及 MR 链接

### 4.2 任务队列与路由
- **共享队列 + 按仓库分组**：任务带仓库标签，worktree 只认领匹配自己仓库的任务
- **原子认领（claim）**：同一任务只被一个 worktree 认领，杜绝重复开发
- **租约（lease）**：worktree 崩溃/超时后任务自动回归队列供他人接手
- **状态机**：`待处理 → 已认领 → 开发中 → 待review → 已提MR / 失败 / 需人工`
- **幂等**：重复扫描不会重复入队或重复建 MR

### 4.3 Worktree 工作池
- 每个 worktree 绑定特定 GitLab 仓库，声明自己的能力（可做哪个仓库）
- 同一仓库可配多个 worktree 组内并行，"谁空谁抢"自动负载均衡
- 定时轮询：空闲时主动认领任务
- **任务间重置**：每任务开始前切回 main、拉最新、基于 main 建新分支，避免脏状态泄漏

### 4.4 开发执行
- 两阶段：先设计定界（判断改哪、改什么性质）→ 再加载对应 spec 分片开发
- **编码走 TDD**：先写失败测试（Red）→ 实现让其通过（Green）→ 重构（Refactor）。测试即需求的可执行表达，也是后续修复的复现载体
- **修复走根因驱动**：闸门失败先诊断根因、判断方向，再定向修复（先写复现测试再修），绝不盲目试错
- 代码风格与现有代码一致，commit message 规范（含 TAPD 需求号）
- 只允许推 feature 分支，禁止直接推 main

### 4.5 质量闸门（替代人工 review 的三道防线）
- **闸门一 · 测试 + HARD 检查**：自动测试 + lint/CI 全局强制，红了则自修，修不好放弃
- **闸门二 · 独立 Review**：全新上下文的 Review Agent 逐条对照 SOFT 规则审查
- **闸门三 · 对抗验证**：专门 Agent 找"这个改动会在什么情况下出错"
- 三道全过才建 MR；任一不过则打回重做或标记需人工

### 4.6 提交 MR（GitLab 集成 · 新建/复用 MCP）
- 自动创建 MR（源分支=需求分支，目标=main）
- MR 描述自动填充：需求描述、验收标准清单、变更文件、TAPD 需求号回链
- MR URL 回写 TAPD

### 4.7 项目规范（Spec）体系 —— 系统可靠性的地基

> **定位：spec 不是启动前置条件，而是规模化的必要条件——用来压低 MR 打回率，让无人值守不翻车。**

- **只管隐性铁律**：架构边界、废弃模块、跨模块约束、安全铁律等"读代码看不出来"的规则。显性风格（命名/格式）交给 linter，不写进 spec
- **薄 spec，从反馈生长**：上线只写 5-10 条已知隐性铁律，其余从 MR 打回中增量沉淀，永远只包含"真实付过代价"的规则
- **两轴分片，按需加载**：
  - **包（结构）分片**：`payment/`、`user/` 等，改哪个包才加载
  - **主题（横切）分片**：security / error-handling / testing 等，按改动性质加载
  - 由设计阶段的定界结果动态决定加载哪些分片
- **强制力分级**：
  - **HARD**：能机械判定的规则 → 编译成 lint/CI 检查，**全局强制、与加载无关**（兜住定界失误）
  - **SOFT**：判断性规则 → prompt 注入 + Review Agent 审查
  - 可靠性指标 = HARD 规则占比
- **永不分片的核心层**：全局红线 + 普适安全铁律，永远常驻注入 + 强制
- **updateSpec 闭环**：
  - 触发点是**异步消费 MR review / CI 反馈**（不是任务最后一环——那时反馈还没到）
  - 优先把规则升级成 HARD 机器检查，其次才写 prose spec
  - 回写时**归片**（包本地规则进包分片，横切规则进主题分片）
  - 产物是一个**人工确认的 spec 变更 MR**（Agent 只能提议，不能自己偷改约束自己的规则）
  - 规范会自动"升级"：一次性教训 → 反复验证有效 → 升为铁律/机器检查
- **冷启动**：从现有代码 + 历史 MR 打回评论逆向提炼初版

### 4.8 触发与通知
- 定时触发（如工作日早 9 点）+ TAPD Webhook 触发（需求评审通过时）
- 全流程结构化留痕：拉取需求、设计方案、改动文件、测试输出、review 结论、MR 链接
- 关键节点推送企微/Slack：MR 已创建、需求需人工介入

---

## 五、安全红线（硬性约束）

| 绝不自动化 | 实现方式 |
|-----------|---------|
| 自动 merge 到主干 | 系统终点是 MR，审批权留给人 |
| 直接推 main 分支 | permission deny 规则 |
| 改生产配置 / 数据库迁移 / CI 密钥 | permission deny 规则 |
| 访问生产环境（kubectl/terraform 等） | permission deny 规则 |
| 处理复杂/模糊需求 | 拉取阶段过滤 |
| Agent 自改 HARD spec / 安全核心 | 只能提议，必须人工确认 |

---

## 六、技术栈（基于 Grok Build 现有能力）

| 能力 | 复用 Grok Build | 需新建 |
|------|----------------|--------|
| 需求拉取 | — | **TAPD MCP Server** |
| MR 创建 | — | **GitLab MCP**（可复用开源 mcp-server-gitlab） |
| 开发循环 | Turn Loop + Tools | — |
| Worktree 隔离 | `--worktree` + `x.ai/git/worktree/*` | — |
| Spec 分层注入 | AGENTS.md 按路径分层（§16） | 分片结构 + 定界逻辑 |
| 上下文控制 | Compaction（§05） | — |
| 编排 | Workflow 脚本（pipeline/parallel） | 编排脚本 |
| 无界面运行 | Headless / Agent ACP 模式 | — |
| 质量闸门 | 独立 Agent 对抗式验证（§14） | — |
| 状态/队列 | — | **状态存储**（SQLite/Redis，含 claim/lease） |

---

## 七、非功能需求

- **可靠性**：单需求失败不影响其他；崩溃可恢复（任务回归队列）
- **可观测性**：无人值守全程留痕、异常主动通知
- **渐进上线**：初期 `max_stories=1` 只吃最简单需求，跑通后放量
- **可升级**：闸门失败先根因分析、定向修复（走 TDD），不盲目试错；仅当诊断确认走不通时带着完整诊断报告升级求助，绝不死磕改坏代码
- **可度量**：以 HARD 规则占比、MR 打回率衡量各仓库"自主开发就绪度"

---

## 八、实施路线

| 阶段 | 交付 | 验证 |
|------|------|------|
| 1 | TAPD MCP（拉需求） | 能列出并正确筛选/路由待开发需求 |
| 2 | GitLab MCP | 能在测试仓库建 MR |
| 3 | 单需求串通 Workflow | 一个简单需求全自动出 MR |
| 4 | Spec 体系 v0（薄 spec + HARD 检查 + 分片加载） | 违反隐性铁律被 CI 挡下 |
| 5 | 三道质量闸门 + 放弃逻辑 | 坏需求被正确放弃 |
| 6 | 队列 + 路由 + claim/lease + 幂等 | 多需求分组并行、不重复、可恢复 |
| 7 | updateSpec 闭环（异步吃反馈 → 归片 → 人审 MR） | 打回的问题能沉淀为规范并生效 |
| 8 | 定时/Webhook 触发 + 通知 | 无人值守自动跑完并通知 |

---

## 九、核心设计理念（一句话）

**能力上自己从需求跑到 MR；安全上绝不碰红线；质量上三道独立闸门把关（HARD 机器强制 + SOFT 独立 review）；规范上用"薄 spec + 按包/主题分片 + updateSpec 闭环"让系统每跑一个需求就变可靠一点——最终裁决权留给 MR 前的那个人。**

---

# 附录 A：技术实现方案（怎么用 Grok Build 落地）

> 本附录回答"具体怎么开发、用到项目里哪些东西"。所有命令/接口均来自 Grok Build 用户指南实测，非杜撰。

## A.1 复用 Grok Build 的哪些能力（对应关系）

| 系统能力 | Grok Build 现成机制 | 出处 |
|---------|-------------------|------|
| 无界面执行一个需求 | `grok -p "<prompt>"` headless 模式 | 14-headless-mode |
| 自动批准工具（无人值守） | `--yolo` / `--permission-mode bypassPermissions` | 14 |
| 安全红线（禁 push main 等） | `--deny "Bash(...)"`（可重复，deny 优先） | 14 |
| 迭代上限（防死循环兜底） | `--max-turns <N>` | 14 |
| 结构化结果（拿 sessionId/成本/停止原因） | `--output-format json` | 14 |
| worktree 隔离 | `--worktree=<name>` + `--ref main` | 14 / 08 |
| **测试不过就不让停（核心质量门）** | **`Stop` hook 返回 `{"decision":"block"}`** | 10-hooks |
| **根因分析/继续修（非盲目）** | Stop hook 反馈 reason 回喂模型，最多 8 次续跑后强停 | 10 |
| 独立 Review（全新上下文） | `spawn_subagent`（子代理独立上下文）或独立一次 `grok -p` | 16-subagents |
| 对抗验证 | 再 spawn 一个 `explore`/`general-purpose` 子代理 | 16 |
| 拉需求（TAPD） | **新建 TAPD MCP Server**（stdio） | 07-mcp |
| 建 MR（GitLab） | 现成 `@modelcontextprotocol/server-gitlab`（stdio MCP） | 07 |
| 项目规范注入 | `AGENTS.md`（按路径分层）+ `.grok/config.toml`（repo 级） | 12 / 07 |
| 规范分片加载 | 目录级 AGENTS.md（越深越优先）+ 定界后 `@` 引用对应 spec | 01 / 12 |
| 通知 | `SessionEnd` / `Notification` hook（type=http 或 command） | 10 |
| 恢复/续跑 | `--resume <id>` / `--continue` | 14 |
| 认证（CI 无浏览器） | `XAI_API_KEY` 环境变量 | 14 |

## A.2 系统组件与部署形态

无人值守系统 = **一个外部编排器（守护进程/定时器）** + **反复调用的 `grok -p` 子进程** + **两个 MCP Server** + **仓库内的 hook/spec/config**。Grok 本身不常驻，每个任务起一个 headless 进程。

```
外部编排器（Python/Node 常驻进程，非 Grok）
  ├─ 定时扫描器：调 TAPD MCP 拉需求 → 写状态库（SQLite）
  ├─ worktree 工作池：每个 worktree 一个循环，claim 任务 → 起 grok -p
  └─ 反馈消费器：异步读 MR review/CI → 触发 updateSpec

每个任务执行 = 一次 grok -p 子进程：
  grok -p "<需求 prompt>" \
    --cwd <worktree 路径> \
    --yolo \
    --max-turns 40 \
    --output-format json \
    --deny "Bash(git push*origin*main*)" \
    --deny "Bash(git push --force*)" \
    --deny "Bash(rm -rf*)" \
    --deny "Bash(kubectl*)" --deny "Bash(terraform*)"
  # TAPD/GitLab MCP、Stop 门禁 hook、AGENTS.md 均由仓库内配置自动加载
```

## A.3 关键实现点逐一说明

### ① TAPD MCP Server（唯一必须从零写的组件）
- stdio MCP，暴露 `tapd_list_stories`（拉需求，读"所属仓库"字段）、`tapd_update_story`（回写状态+MR链接）
- 注册到 repo 级 `.grok/config.toml` 的 `[mcp_servers.tapd]`，secret 用 `${TAPD_TOKEN}` 环境变量引用
- 大输出自动落盘（MCP 20KB 截断机制，见 07），不撑爆上下文

### ② GitLab MR：直接用现成 MCP
- `grok mcp add gitlab -e GITLAB_TOKEN=${GITLAB_TOKEN} -- npx -y @modelcontextprotocol/server-gitlab`
- 或写进 `.grok/config.toml` 的 `[mcp_servers.gitlab]`
- Agent 通过 `search_tool`/`use_tool` 调 `gitlab__create_merge_request`

### ③ 三道质量闸门的落地映射
- **闸门一（测试+HARD 检查）= `Stop` hook 门禁**：这是最关键的落地技巧。写一个 `.grok/hooks/stop-gate.json` → `Stop` 事件 → 跑 `bin/verify.sh`（测试 + lint）→ 不过则 `{"decision":"block","reason":"..."}`，Grok 会把 reason 回喂模型继续修。**这天然实现了"检查失败→回去修"的回环，且 Grok 内置"8 次续跑后强停"作为死循环兜底**（见 10-hooks / Stop Decision Control）
- **闸门二（独立 review）**：主 Agent 用 `spawn_subagent`（`capability_mode: read-only`，全新上下文）审 SOFT 规则；或编排器在 MR 前单独起一次 `grok -p "review this diff against .grok/spec/"`
- **闸门三（对抗验证）**：再 spawn 一个子代理专门找"会在什么情况下出错"

### ④ 根因驱动 + 升级边界的落地
- **根因驱动**：靠 Stop hook 的 reason 文案引导——reason 里明确要求"先诊断根因再改，不要盲目试错"
- **升级边界**：三层——(a) Stop hook 检测"无进展"（`stopHookActive` + 连续同错）主动 `{"continue":false,"stopReason":"..."}` 强停；(b) `--max-turns` 兜底；(c) 编排器超时兜底。任一触发 → 标记需求=需人工 + 附诊断（读 `--output-format json` 的结果）

### ⑤ Spec 分片注入的落地
- **核心层（永远注入）**：仓库根 `AGENTS.md` + `.grok/config.toml` 的红线
- **包分片**：各包目录下放 `AGENTS.md`（Grok 按路径分层，越深越优先，见 12），改到哪个包自动带上
- **主题分片**：`.grok/spec/security.md` 等，定界后在 prompt 里 `@.grok/spec/security.md` 显式引用
- **HARD 检查全局**：lint/CI 在 `bin/verify.sh` 里，被 Stop hook 调用，与 Agent 加载了哪些 spec 无关

### ⑥ 队列/claim/lease/状态机
- Grok 不管这层——由**外部编排器 + SQLite/Redis** 实现（见正文 4.2）
- worktree 池：编排器为每个 `repo × 并行度` 维护一个 worktree（`grok --worktree` 或直接 `git worktree add`），循环里 claim → 起 `grok -p --cwd <worktree>`

### ⑦ updateSpec 闭环的落地
- 编排器的反馈消费器异步读 GitLab MR 评论 + CI 结果
- 起一次 `grok -p "归纳这些 review 反馈，判断哪些该升级为 lint 规则、哪些写入哪个 spec 分片，产出一个改 .grok/spec 或 lint 配置的 diff"` → 生成 spec 变更 MR → 人工审

## A.4 最小可跑闭环（阶段 1-3 的实际命令）

```bash
# 0. 认证（CI）
export XAI_API_KEY="xai-..."

# 1. 装 GitLab MCP（现成）
grok mcp add gitlab -e GITLAB_TOKEN=$GITLAB_TOKEN -- npx -y @modelcontextprotocol/server-gitlab

# 2. TAPD MCP 写进 repo 的 .grok/config.toml（需自建 server）
# [mcp_servers.tapd]
# command = "node"; args = ["tapd-mcp.js"]; env = { TAPD_TOKEN = "${TAPD_TOKEN}" }

# 3. Stop 门禁 hook：.grok/hooks/stop-gate.json（测试+lint 不过就不让停）

# 4. 单需求全自动跑（编排器对每个 claim 到的任务执行）：
grok -p "$(cat <<'EOF'
开发 TAPD 需求 #12345：<标题>
验收标准：<...>
流程：走 TDD（先写失败测试→实现→重构）；改动限定在相关包；
完成后用 gitlab__create_merge_request 建 MR，回填 TAPD。
闸门失败时先根因分析找对方向再改，不要盲目试错。
EOF
)" \
  --cwd /worktrees/payment-1 \
  --yolo --max-turns 40 --output-format json \
  --deny "Bash(git push*main*)" --deny "Bash(rm -rf*)" \
  > result.json

# 5. 读结果判断成功/升级
jq -r '.stopReason' result.json   # end_turn=完成；其它=可能需人工
```

## A.5 落地风险与对策

| 风险 | 对策 |
|------|------|
| Stop hook 死循环 | Grok 内置 8 次续跑上限 + `--max-turns` + 编排器超时，三重兜底 |
| MCP 输出撑爆上下文 | Grok 默认 20KB 截断落盘（`GROK_MAX_MCP_OUTPUT_BYTES` 可调） |
| 项目 hook 不生效 | 项目 hook 需 `--trust` / `/hooks-trust`（folder-trust 门禁） |
| worktree 脏状态 | 每任务前 `git worktree` 重置到干净 main（编排器负责，见 4.3） |
| 密钥泄漏 | MCP config 用 `${VAR}` 引用，不写明文；`.grok/config.toml` 提交前审 |
| CI 无浏览器认证 | `XAI_API_KEY` + `GROK_DISABLE_AUTOUPDATER=1` + 只读挂载 `~/.grok` |
